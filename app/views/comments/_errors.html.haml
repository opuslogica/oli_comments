- if obj.errors.any?
  #error_explanation.wrapper
    %h5
      %u= "#{pluralize(obj.errors.count, "error")} prohibited this object from being saved:"
    %ul
      - obj.errors.full_messages.each do |msg|
        %li= msg